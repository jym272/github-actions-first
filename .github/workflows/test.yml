name: Test frontend
on: [ push ]

jobs:
  test-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      #      - name: Prepare environment #already ubuntu has nodejs
      #        uses: actions/setup-node@v1
      #        with:
      #          node-version: 'lts/hydrogen'
      - run: npm ci
        working-directory: frontend
      - run: npm run test
        working-directory: frontend
